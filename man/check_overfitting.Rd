% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metaoverfit.R
\name{check_overfitting}
\alias{check_overfitting}
\title{Full overfitting check for meta-regression}
\usage{
check_overfitting(yi, vi, mods, data, B = 500, conf_level = 0.95,
  parallel = FALSE)
}
\arguments{
\item{yi}{Vector of effect sizes, or an unquoted variable name in \code{data}.}

\item{vi}{Vector of sampling variances, or an unquoted variable name in \code{data}.}

\item{mods}{A one-sided formula (e.g. \code{~ x1 + x2}) or a model matrix.}

\item{data}{Data frame containing the variables referenced in \code{yi},
  \code{vi}, and \code{mods}.}

\item{B}{Number of bootstrap replicates (default 500).}

\item{conf_level}{Confidence level (default 0.95).}

\item{parallel}{Logical. Use parallel processing? Default \code{FALSE}.}
}
\value{
An object of class \code{"metaoverfit_check"}.
}
\description{
A convenience wrapper that runs both LOOCV and bootstrap
  analyses, returning a single \code{metaoverfit_check} object with a
  formatted \code{print} method.
}
\examples{
\dontrun{
library(metafor); library(metadat)
data(dat.bcg)
dat <- escalc(measure = "RR", ai = tpos, bi = tneg,
              ci = cpos, di = cneg, data = dat.bcg)
res <- check_overfitting(yi, vi, mods = ~ablat, data = dat, B = 200)
print(res)
}
}
